# Configuration variables for the requestor node

DATA_DIR=requestor_data
KEY_NAME=test1-key

CENTRAL_NET_HOST=127.0.0.1:7477

# Defaults for direct mode: daemon and agent connect to market API
# directly, agent connects to daemon directly
MARKET_URL_BASE=http://127.0.0.1:5001
GSB_URL=tcp://127.0.0.1:6010
YAGNA_API_URL=http://127.0.0.1:6000

if [ -n "$USE_PROXY" ]; then
    # Proxy mode: daemon and agent connect to market API
    # through proxy (using distinct ports); agent connects
    # to daemon through proxy
    if [ -n "$AGENT" ]; then
        MARKET_URL_BASE=http://127.0.0.1:15002
        YAGNA_API_URL=http://127.0.0.1:16000
    else
        MARKET_URL_BASE=http://127.0.0.1:15001
    fi
fi

YAGNA_ACTIVITY_URL=${YAGNA_API_URL}/activity-api/v1/
YAGNA_MARKET_URL=${YAGNA_API_URL}/market-api/v1/
CENTRAL_MARKET_URL=${MARKET_URL_BASE}/market-api/v1/

# Payment config
CHAIN=mainnet
GETH_ADDRESS=http://localhost:8545
GNT2_CONTRACT_ADDRESS=0xFDFEF9D10d929cB3905C71400ce6be1990EA0F34
REQUIRED_CONFIRMATIONS=1
YAGNA_KEY_FILE=../asset/key/001.json
